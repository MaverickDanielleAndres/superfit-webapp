version: '3.9'

services:
  superfit:
    build:
      context: .
      dockerfile: Dockerfile
    image: superfit:local
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
